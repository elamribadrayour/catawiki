INSERT INTO
    products
VALUES
    (
        ? :: INTEGER,
        ? :: TEXT,
        ? :: BIGINT,
        ? :: TEXT,
        ? :: TEXT,
        ? :: TEXT,
        ? :: TEXT,
        ? :: INT,
        ? :: TEXT,
        ? :: BOOLEAN,
        ? :: TEXT,
        ? :: TEXT,
        ? :: BIGINT,
        ? :: TEXT,
        ? :: BOOLEAN,
        ? :: BOOLEAN,
        ? :: BOOLEAN,
        ? :: TIMESTAMP,
        ? :: TEXT,
        ? :: TEXT,
        ? :: BIGINT,
        ? :: TEXT,
        (
            ? :: BIGINT,
            ? :: BOOLEAN,
            (? :: DOUBLE, ? :: DOUBLE, ? :: DOUBLE),
            ? :: BIGINT,
            ? :: BIGINT,
            ? :: TEXT,
            ? :: INT,
            ? :: TEXT,
            ? :: TEXT,
            ? :: BOOLEAN
        )
    )